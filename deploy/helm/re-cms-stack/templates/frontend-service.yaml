apiVersion: v1
kind: Service
metadata:
  name: {{ include "re-cms-stack.fullname" . }}-frontend
spec:
  selector:
    app.kubernetes.io/name: re-cms-admin
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
